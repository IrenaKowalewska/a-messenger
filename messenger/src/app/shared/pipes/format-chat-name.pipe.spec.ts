import { FormatChatNamePipe } from './format-chat-name.pipe';

describe('FormatChatNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatChatNamePipe();
    expect(pipe).toBeTruthy();
  });
});
